import React from "react";
import styles from "./card_form.module.css";

const CardForm = ({ card }) => {
  // console.log(card);
  return <div className={styles.form}>에디터 영역입니다</div>;
};

export default CardForm;
